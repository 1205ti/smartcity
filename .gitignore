# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Env / secrets
.env
.env.*
*.key
*.pem
credentials.json

# macOS
.DS_Store

# Data / outputs (필요하면 화이트리스트로 풀어 쓰세요)
data/raw/
*.gpkg-journal
*.parquet.tmp

# OMC 운영 아티팩트
.omc/
!.omc/skills/

# Node / wrangler
node_modules/
.wrangler/

.dev.vars*
!.dev.vars.example
!.env.example

# 강의 실습 산출물은 커밋한다 — 결과를 바로 볼 수 있어야 하므로.
# 다만 원본 데이터는 용량이 커서 제외한다 (내려받는 방법은 각 노트북 첫 셀에).
data/raw/
